/**
 * Definition for singly-linked list.
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */

//Strategy : Find the count of list A
             Find the count of list B
             Find the diff bewtween listA and list B
             if list A is longer move it(A pointer) by diff number of times
             if list B is longer move it(B pointer) by diff number of times
             After moving both the pointers now start traversing and find the intersectign node

/**
//Re did on 17th Aug 2026

 * @param {ListNode} headA
 * @param {ListNode} headB
 * @return {ListNode}
 */
var getIntersectionNode = function (headA, headB) {

    let countA = 0;
    let countB = 0;
    let diff = 0;
    let cur1 = headA;
    let cur2 = headB;
    let m;
    let n;



    while (cur1) {
        countA++;
        cur1 = cur1.next;
    }

    while (cur2) {
        countB++;
        cur2 = cur2.next;
    }

    if (countA > countB) {
        diff = countA - countB;
        for (var i = 0; i < diff; i++) {
            headA = headA.next
        }

    } else {
        diff = countB - countA;
        for (var i = 0; i < diff; i++) {
            headB = headB.next
        }
    }

    cur1 = headA;
    cur2 = headB;

    console.log("headA::",headA)
    console.log("headB::",headB)

    while (cur1 && cur2) {
        if (cur1 == cur2) {
            return cur1
        }
        cur1 = cur1.next;
        cur2 = cur2.next;
    }



};
