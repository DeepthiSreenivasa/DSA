/**
 * @param {number} x
 * @return {number}
 */
var mySqrt = function (x) {

    let i = 0;
    let sqrt = 0;

    while (sqrt <= x) {
        i++;
        sqrt = i * i;
        if (sqrt == x) {
            return i;
        }
    }

    return i - 1;

};
