/*
 * 
 * Author: Deepthi S
 * Notes :
 * 
 * Startegy : use recuursion - Base Case (print numbers upto the coutn passed to the function)  
                                          + Recursion Case (increase the number by 1 and call the function again)
 * 
 */

function printNumbersIncreasing(num) {

    let n = 10;

    if (num > n) return;

    console.log(num);
    num = num + 1;
    printNumbers(num)
        

   
}

printNumbers(1)

